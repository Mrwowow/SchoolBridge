import { Loader } from '@/components/ui';

export const metadata = {
  title: 'Loader Preview',
};

// Standalone page to watch the looping SchoolBridge loader animation.
export default function LoaderPreviewPage() {
  return <Loader />;
}
