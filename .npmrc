# Hoist type packages to the workspace root so TypeScript can resolve and name
# inferred types portably across pnpm's isolated store (avoids TS2742).
public-hoist-pattern[]=*@types*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
